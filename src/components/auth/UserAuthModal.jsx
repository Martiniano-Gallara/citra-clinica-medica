import React from 'react';

// Patient authentication and registration have been permanently removed as requested.
// Appointment scheduling is now handled exclusively via WhatsApp.
export const UserAuthModal = () => null;

export default UserAuthModal;
